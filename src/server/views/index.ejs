<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/root.css" />
</head>
<body>
  <header>
    <h1>Welcome to the Homepage</h1>
    <% if (userId) { %>
      <p>Logged in as user ID: <%= userId %></p>
      <a href="/auth/logout">Logout</a>
      <!-- Show "Go to Game" only if the user is logged in -->
      <a href="/game">Go to Game</a>
    <% } else { %>
      <a href="/auth/login">Login</a> | <a href="/auth/register">Register</a>
    <% } %>
  </header>
  <main>
    <p>This is the homepage. Add more dynamic content here!</p>
    <% if (userId) { %>
      <p>Start playing the Bingo game! Click on the "Go to Game" link above to get started.</p>
    <% } else { %>
      <p>Please log in or register to access the game.</p>
    <% } %>
  </main>
</body>
</html>